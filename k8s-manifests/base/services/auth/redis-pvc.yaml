apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-redis-data
  namespace: hackz-giganoto
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-csi