apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chat-redis-data
  namespace: hackz-giganoto
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-csi